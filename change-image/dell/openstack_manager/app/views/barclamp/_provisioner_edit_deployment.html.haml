
%p
  = render :partial => "barclamp/node_selector"

:javascript
  $(document).ready(function(){
    var constraints = { "provisioner-server": { "unique": false, "count": 1 }, "provisioner-base": { "unique": false, "count": -1 } };
    node_selector($('#drag_drop'), constraints);
  });

