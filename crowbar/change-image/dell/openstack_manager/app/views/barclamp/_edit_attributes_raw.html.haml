%p
  %label{:for => "proposal_attributes"} #{t('.title')}
  = link_to t('custom'), proposal_barclamp_path(:id => @proposal.name, :controller => @proposal.barclamp, :dep_raw => @dep_raw, :attr_raw => false), :style => "float: right;"
= find_and_preserve do
  %textarea#proposal_attributes.editor{:name => "proposal_attributes"}
    -# TODO: don't do this anymore (currently working around a bug in the JSON gem)
    -# = JSON.pretty_generate @proposal.raw_data['attributes'][@proposal.barclamp].to_hash
    = JSON.pretty_generate JSON.parse(@proposal.raw_data['attributes'][@proposal.barclamp].to_json)

