source 'https://rubygems.org'

group :development do
  gem 'pry-debugger', '>= 0.2'

  gem 'rb-inotify', '>= 0.9'
  gem 'guard', '>= 1.8'

  gem 'guard-rsync', 
    :github => 'aspiers/guard-rsync', 
    :branch => 'crowbar'
end
